# Rules

- **Database Updates**: Always ensure that when a user implements an edit or update feature (like modifying a name, email, or any profile field), the corresponding backend logic must immediately and accurately reflect those changes in the database. Never leave form data updates unhandled or mock them out permanently.
